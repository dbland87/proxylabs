{
  "name": "proxylabs-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro check && astro build",
    "build:production": "node scripts/validate-public-env.mjs && pnpm run build && node scripts/check-built-site.mjs",
    "test": "pnpm run build && node scripts/check-built-site.mjs",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "astro": "7.1.0",
    "typescript": "6.0.3"
  }
}
